"use client";

import { motion } from "framer-motion";
import { Info } from "lucide-react";
import type { PredictionResult } from "@/types";

interface Props {
  result: PredictionResult;
}

const INTERPRETATIONS: Record<string, { title: string; body: string }> = {
  low: {
    title: "Low PCOS Risk Detected",
    body:
      "Based on your provided clinical parameters, the AI model indicates a low probability of Polycystic Ovary Syndrome. Your hormonal markers, cycle regularity, and lifestyle factors are within ranges typically associated with lower PCOS risk.",
  },
  moderate: {
    title: "Moderate PCOS Risk Detected",
    body:
      "Your assessment results indicate moderate PCOS risk indicators. Several clinical parameters suggest some hormonal or metabolic irregularities. A consultation with a gynecologist for further evaluation — including blood panel and ultrasound — is advised.",
  },
  high: {
    title: "High PCOS Risk Detected",
    body:
      "Your clinical indicators strongly suggest a high likelihood of PCOS. Multiple risk factors including hormonal levels, follicle parameters, and symptom profile are consistent with PCOS patterns. Prompt consultation with an endocrinologist or gynecologist is strongly recommended.",
  },
};

const DISCLAIMER = "This result is generated by an AI model for educational screening purposes only. It is not a medical diagnosis. Please consult a qualified healthcare professional for clinical evaluation.";

export default function RiskSummary({ result }: Props) {
  const interpretation = INTERPRETATIONS[result.riskLevel];

  return (
    <motion.div
      initial={{ opacity: 0, y: 20 }}
      animate={{ opacity: 1, y: 0 }}
      transition={{ duration: 0.5, delay: 0.15 }}
      className="bg-white rounded-2xl border border-slate-100 shadow-sm p-6"
    >
      <h3 className="text-sm font-semibold text-slate-500 uppercase tracking-widest mb-4">
        Clinical Summary
      </h3>

      <div className="mb-4">
        <h4 className="text-lg font-bold text-slate-900 mb-2">{interpretation.title}</h4>
        <p className="text-sm text-slate-600 leading-relaxed">{interpretation.body}</p>
      </div>

      {/* Key metrics */}
      <div className="grid grid-cols-2 gap-3 mb-5">
        <div className="p-3 bg-slate-50 rounded-xl">
          <p className="text-xs text-slate-400 mb-0.5">Prediction</p>
          <p className="text-sm font-bold text-slate-900">
            {result.prediction === 1 ? "PCOS Likely" : "PCOS Unlikely"}
          </p>
        </div>
        <div className="p-3 bg-slate-50 rounded-xl">
          <p className="text-xs text-slate-400 mb-0.5">Assessment Type</p>
          <p className="text-sm font-bold text-slate-900 capitalize">{result.assessmentType}</p>
        </div>
        <div className="p-3 bg-slate-50 rounded-xl">
          <p className="text-xs text-slate-400 mb-0.5">Confidence</p>
          <p className="text-sm font-bold text-slate-900">{result.riskPercent}%</p>
        </div>
        <div className="p-3 bg-slate-50 rounded-xl">
          <p className="text-xs text-slate-400 mb-0.5">Risk Category</p>
          <p className="text-sm font-bold text-slate-900 capitalize">{result.riskLevel}</p>
        </div>
      </div>

      {/* Disclaimer */}
      <div className="flex items-start gap-2 p-3 bg-amber-50 border border-amber-100 rounded-xl">
        <Info className="w-3.5 h-3.5 text-amber-500 mt-0.5 shrink-0" />
        <p className="text-xs text-amber-700 leading-relaxed">{DISCLAIMER}</p>
      </div>
    </motion.div>
  );
}
